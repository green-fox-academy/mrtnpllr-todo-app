#include <iostream>
#include <fstream>
#include "Functions.h"

int main(int argc, char* argv[])
{
    if (argc == 1) {
        return printUsage("print_usage.txt");
    }

    //if (argv[1] == "-l")

    return 0;
}